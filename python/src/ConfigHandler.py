#This will eventually do the following
#1. Check for a config file, and if not found create one with the defaultsettings.py as a reference,
    # export as json

#2. Read the current settings file and store that information as constants that fit into the code
    #the same way the current "hard coded" data from utils.py does.

#3. Update the settings based on user changes, and overwrite the current settings file
    # Java will handle the state of what settings are between menus, but in python I will 
        # process Java's eventual output.
            #I still need to define that schema
